Fix remaining bugs: nav link, dead CSS, expand hint

- Pass onNavigateTab prop to OverviewTab so "View all" button
  correctly switches to Inventory tab via activateTab()
- Remove broken document.querySelector data-tab selector
- Clean up dead expandBtn CSS (replaced by chevronBtn)
- Add subtle "Tap card for rates & details" hint on collapsed cards
